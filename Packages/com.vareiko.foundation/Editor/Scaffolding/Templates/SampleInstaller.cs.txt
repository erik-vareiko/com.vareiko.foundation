using MessagePipe;
using UnityEngine;
using Vareiko.Foundation;
using VContainer;
using VContainer.Unity;

namespace {{NAMESPACE}}
{
    // Attach to a GameObject in a sample scene as the project-scope root.
    public sealed class {{MODULE_NAME}}SampleInstaller : LifetimeScope
    {
        [SerializeField] private {{MODULE_NAME}}Config _config;

        protected override void Configure(IContainerBuilder builder)
        {
            MessagePipeOptions signalOptions = FoundationRuntimeInstaller.InstallProjectServices(builder);
            Foundation{{MODULE_NAME}}Installer.Install(builder, signalOptions, _config);
        }
    }
}
