using Vareiko.Foundation.Signals;

namespace {{NAMESPACE}}
{
    public sealed class {{MODULE_NAME}}Service : I{{MODULE_NAME}}Service, VContainer.Unity.IInitializable
    {
        private readonly IFoundationSignalBus _signalBus;

        public {{MODULE_NAME}}Service(IFoundationSignalBus signalBus)
        {
            _signalBus = signalBus;
        }

        public bool IsReady { get; private set; }

        public void Initialize()
        {
            IsReady = true;
            _signalBus.Publish(new {{MODULE_NAME}}InitializedSignal());
        }
    }
}
